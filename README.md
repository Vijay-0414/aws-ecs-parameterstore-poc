# aws-ecs-parameterstore-poc
POC project to demonstrate secure configuration handling in AWS ECS
# AWS ECS Parameter Store POC

This is a simple POC project created to demonstrate:
- Clean GitHub repository setup
- No hardcoded secrets
- Environment-based configuration

Secrets will be injected at runtime.
